---
version: 1
applications:
  - name: s3-interface
    memory: 128M
    instances: 1
    buildpacks:
      - python_buildpack
    no-route: true
    health-check-type: process
    services:
     - sk-s3
